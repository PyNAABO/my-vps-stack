# Dozzle

**Real-time Docker Logs**

A simple, lightweight application that provides a web-based interface to monitor your Docker container logs in real-time.

## 🚀 Usage

- **URL:** `https://logs.your-domain.com`
- **Default Credentials:** `admin` / `adminadmin1234` (Configured in `docker-compose.yml`)

## ⚙️ Configuration

- **Authentication:** Basic Auth configured via `DOZZLE_AUTH_USERNAME` and `DOZZLE_AUTH_PASSWORD`.
